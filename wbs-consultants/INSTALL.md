# Quick Installation Guide

## Install EmailJS Package

Run this command in the `wbs-consultants` directory:

```bash
npm install @emailjs/browser
```

## That's it!

Now follow the setup instructions in `SETUP_COMPLETE.md`
